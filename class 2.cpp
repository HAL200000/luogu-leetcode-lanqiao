#include <bits/stdc++.h>
using namespace std;

int main()
{
   //
   stack <int> s;
   s.push(a);
   s.pop();
   s.top();
   s.size();
   s.empty();

   queue <int> q;
   q.push(a);
   q.pop();
   q.front();
   q.back();
   q.size();
   q.empty();

   set <int> ds;
   ds.insert(x);
   ds.erase(x);
   ds.empty();
   ds.size();

   




   system("pause");
   return 0;
}
